import { memo } from "react";
import { Bath, BedDouble, Check, Heart, MapPin, Ruler } from "lucide-react";
import { Button } from "@/components/ui/button";
import type { Listing } from "@/types/property";
const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);
const posted=(value?:string)=>{if(!value)return "Recently";const days=Math.max(0,Math.floor((Date.now()-Date.parse(value))/86400000));return days===0?"Today":`${days} days ago`};
export const PropertyCard=memo(function PropertyCard({listing,favorite,onFavorite,onOpen}:{listing:Listing;favorite:boolean;onFavorite:()=>void;onOpen:()=>void}){return <article className="group overflow-hidden rounded-lg border border-border bg-card shadow-card transition duration-300 hover:-translate-y-1 hover:shadow-card-hover"><div role="button" tabIndex={0} onClick={onOpen} onKeyDown={(e)=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();onOpen()}}} aria-label={`View ${listing.apartment_name}`} className="cursor-pointer focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><div className="relative aspect-[4/3] overflow-hidden bg-muted"><img src={listing.image} alt={`${listing.apartment_name} property`} width={1200} height={800} loading="lazy" className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.035]"/><div className="absolute left-3 top-3 flex gap-2">{listing.is_live&&<span className="rounded-sm bg-live px-2 py-1 text-[10px] font-bold tracking-[.12em] text-live-foreground">LIVE</span>}{listing.is_verified&&<span className="flex items-center gap-1 rounded-sm bg-card/95 px-2 py-1 text-[10px] font-bold tracking-[.08em] text-primary"><Check className="size-3"/>VERIFIED</span>}</div><Button variant="secondary" size="icon" aria-label={favorite?"Remove from favorites":"Add to favorites"} aria-pressed={favorite} onClick={(e)=>{e.stopPropagation();onFavorite()}} className="absolute right-3 top-3 rounded-full bg-card/95 shadow-sm"><Heart className={favorite?"fill-primary text-primary":""}/></Button></div><div className="p-5"><p className="text-xs font-medium text-primary">{listing.property_type}</p><h2 className="mt-1 truncate font-display text-xl font-semibold">{listing.apartment_name}</h2><p className="mt-2 flex items-center gap-1.5 text-sm text-muted-foreground"><MapPin className="size-3.5"/>{listing.locality}, Chennai</p><p className="mt-5 font-display text-2xl font-semibold">{money(listing.price)}</p><div className="mt-4 flex flex-wrap gap-x-4 gap-y-2 border-y border-border py-3 text-xs text-muted-foreground"><span className="flex items-center gap-1.5"><BedDouble className="size-4"/>{listing.bedroom} BHK</span>{listing.bathroom!=null&&<span className="flex items-center gap-1.5"><Bath className="size-4"/>{listing.bathroom} Bath</span>}{listing.carpet_area!=null&&<span className="flex items-center gap-1.5"><Ruler className="size-4"/>{listing.carpet_area.toLocaleString("en-IN")} sqft</span>}</div><div className="mt-4 flex justify-between text-xs text-muted-foreground"><span>Posted by <b className="font-medium text-foreground">{listing.posted_by??"—"}</b></span><span>{posted(listing.posted_at)}</span></div></div></div></article>});
